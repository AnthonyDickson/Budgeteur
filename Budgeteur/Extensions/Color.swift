//
//  Color.swift
//  Budgeteur
//
//  Created by Anthony Dickson on 7/01/23.
//

import Foundation
import SwiftUI

extension Color {
    /// A green colour suited for backgrounds in `light` colour schemes.
    static let moneyGreen = Color(red: 0.49, green: 0.96, blue: 0.49)
    /// A green colour suited for backgrounds in`dark` colour schemes.
    static let moneyGreenDarker = Color(red: 0.38, green: 0.72, blue: 0.38)
    
    /// A red colour suited for backgrounds in `light` colour schemes.
    static let grapefruitRed = Color(red: 1, green: 0.6, blue: 0.6)
    /// A red colour suited for backgrounds in `dark` colour schemes.
    static let bloodOrange = Color(red: 0.76, green: 0.39, blue: 0.39)
}
